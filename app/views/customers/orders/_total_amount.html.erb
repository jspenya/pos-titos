<% if @total_order_amount > 0 %>
  <div class="mt-3 mb-3">
    <div>
      <p>Total Amount</p>
      <b class="total-amount"><%= number_to_currency(@total_order_amount, unit: "₱") %></b>
    </div>
  </div>
<% end %>
