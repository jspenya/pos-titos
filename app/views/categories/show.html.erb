<%= link_to sanitize("&larr; Back to Categories"), categories_path %>

<h2 class="page-title"><%= @category.name %></h2>

<%= render @products %>
<%= link_to 'New Product', new_category_product_path(@category), class: 'btn btn-dark' %>
